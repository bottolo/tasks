meta {
  name: Create Task
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/tasks
  body: json
  auth: inherit
}

body:json {
  {
     "title":"Adding a simple task!",
     "description":"Desc",
     "completed":false
  }
}
